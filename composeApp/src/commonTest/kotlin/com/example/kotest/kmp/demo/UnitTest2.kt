package com.example.kotest.kmp.demo

import kotlin.test.Test
import kotlin.test.assertEquals

class UnitTest2 {
    @Test
    fun example21() {
        println("ℹ️ kotest demo example unit test 21 running on ${getPlatform().name}")
        assertEquals(3, 1 + 2)
    }

    @Test
    fun example22() {
        println("ℹ️ kotest demo example unit test 22 running on ${getPlatform().name}")
        assertEquals(3, 1 + 2)
    }
}
